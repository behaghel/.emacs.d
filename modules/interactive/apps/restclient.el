;;; restclient.el --- REST client app configuration -*- lexical-binding: t; -*-

;;; Commentary:
;; Interactive REST request editing and execution.

;;; Code:

(use-package restclient
  :commands (restclient-mode))

(provide 'apps/restclient)
;;; restclient.el ends here
