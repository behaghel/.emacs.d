;;; setup-completion.el --- wrapper for migrated module -*- lexical-binding: t; -*-

;;; Commentary:
;; Backwards-compatible loader for migrated completion stack.

;;; Code:

(require 'completion/core)

(provide 'setup-completion)
;;; setup-completion.el ends here
