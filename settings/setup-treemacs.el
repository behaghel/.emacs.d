;;; setup-treemacs.el --- wrapper for migrated module -*- lexical-binding: t; -*-

;;; Commentary:
;; Backwards-compatible loader for migrated Treemacs configuration.

;;; Code:

(require 'hub/navigation/treemacs)

(provide 'setup-treemacs)
;;; setup-treemacs.el ends here
