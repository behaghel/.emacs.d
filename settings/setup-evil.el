;;; setup-evil.el --- wrapper for migrated module -*- lexical-binding: t; -*-

;;; Commentary:
;; Backwards-compatible loader for migrated Evil configuration.

;;; Code:

(require 'hub/editing/evil)

(provide 'setup-evil)
;;; setup-evil.el ends here
