#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)"

cd "$repo_root"
exec devenv shell --secretspec-provider "env://BYPASS" -- env HOME=$PWD emacs --batch -Q -L . -l scripts/approval-refresh-overdrive-page1.el --eval '(hub/script-export-approval-refresh-overdrive-page1 default-directory)'
